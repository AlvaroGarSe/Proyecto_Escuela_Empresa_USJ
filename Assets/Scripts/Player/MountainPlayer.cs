using UnityEngine;

public class MountainPlayer : PlayerInputController
{
    // Script state [IN PROGRESS]

    // Script done for managing the behaviour of the player on the Mountain Game
    // Since the gameplay will only work by jumping. It's only needed to program the movement while jumping.
    // Counting how much time the "Jump" button has been pressed
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
